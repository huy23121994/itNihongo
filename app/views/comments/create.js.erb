$("#review-<%= @review.id %> #comments").append("<%= j render('comments/comment', comment: @comment) %>")// them 1 set
$("#review-<%= @review.id%> #comment_content").val("");
$("#review-<%= @review.id %> .reply_area a:first-child").html('Bình luận (<%= @review.comments.count %>)')