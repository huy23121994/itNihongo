<li id="comment-<%= comment.id %>">
    <div class="media">
      <div class="media-left">
        <a href="#">
          <img class="media-object profile_img" src="<%= comment.user.avatar.url %>" width="45">
        </a>
      </div>
      <div class="media-body">
        <h5 class="media-heading text-capitalize pull-left"><strong><%= comment.user.username %></strong></h5>
        <span class="timestamp"> <%= time_ago_in_words(comment.created_at) %> ago</span>
        <% if signed_in? %>
    	    <% if current_user?(comment.user) || current_user?(comment.review.user)%>
    	         <%= link_to raw("<i class='glyphicon glyphicon-trash'></i> Xóa"), comment, method: :delete,data: { confirm: "You sure?" },remote:true,class: "pull-right" %>
    	    <% end %>
        <% end %>
        <div class="clearfix"></div>
        <pre class="text-f-uppercase"><%= comment.content %></pre>
      </div>
    </div>
</li>
