
	<%= form_for(current_user.comments.build, remote: true) do |f| %>
	    <%= f.text_area :content, class: 'full_width', id: 'comment_content' ,placeholder: "Để lại lời nhận xét...", required: "" %>
	    <%= hidden_field_tag :review_id, review.id %>
		<%= f.submit "Gửi bình luận của bạn", class: "btn btn-primary" %>
	<% end %>